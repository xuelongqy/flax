# @flax/cli

Intended responsibility: Future developer commands for project setup, bundling, and
local development.

Status: empty ESM module scaffold. TypeScript compilation emits JavaScript and
declarations but no public runtime API. The package is private and version 0.0.0.

No executable is registered. Node-specific tooling dependencies will be added when CLI
behavior is implemented.

See [architecture](../../docs/architecture/README.md) and
[scoped checks](../../CONTRIBUTING.md#checks).
