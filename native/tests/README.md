# Native Tests

No native runtime or engine tests exist yet.

Add tests here when native behavior exists, including ownership, callbacks, exceptions,
shutdown, and ABI integration. Keep engine compatibility scenarios coordinated with the
[cross-package tests](../../tests/compatibility/README.md).

CMake configuration is a toolchain check, not runtime coverage.
