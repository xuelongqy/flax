# Cross-Package Tests

[compatibility](compatibility/README.md) reserves shared runtime behavior scenarios.
Package unit and widget tests belong next to their respective implementations; native
tests belong in the native tree.

No framework tests exist yet. The implemented documentation-checker tests live with the
[maintenance tool](../tool/README.md).
